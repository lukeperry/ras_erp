# URL of your local Frappe bench site, used only by `vite dev` to proxy
# /api requests so the dashboard and Frappe are effectively same-origin
# during development too (no CORS setup needed).
FRAPPE_SITE_URL=http://ras-erp.localhost:8000
